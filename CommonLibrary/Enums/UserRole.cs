namespace CommonLibrary.Enums
{
    /// <summary>
    /// Enum representing different user roles in the system.
    /// </summary>
    public enum UserRole
    {
        TRUCKING_COMPANY,    // Represents a trucking company
        CLEARING_AGENT,      // Represents a clearing agent
        CARGO_INSURANCE,     // Represents a cargo insurance entity
        FREIGHT_FORWARDER,   // Represents a freight forwarder
        CARGO_OWNER,         // Represents a cargo owner
        NSTAR_DRIVER,        // Represents an NSTAR driver
        OWNER,               // Represents the owner role
        CARGO_OWNER_COMP,    // Represents a cargo owner company
        ADMIN                // Represents an administrator
    }
}
